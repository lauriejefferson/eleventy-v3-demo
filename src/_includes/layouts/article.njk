---
layout: "layouts/main.njk"
---

<h1 class="article__title">{{ title }}</h1>
<url class="tags__list">
  {%- for tag in tags %} {% if tag != 'post' %}
  <li class="tags__list-item">
    <a href="{{ '/tags/' | url }}{{ tag }}" class="tag">{{ tag }}</a>
  </li>
  {% endif %} {% endfor -%}
</url>
{{ content | safe }}
